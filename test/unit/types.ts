export interface DocumentTypes {
  aDocument: {
    aProperty: string;
  };
  anotherDocument: {
    bProperty: number;
  };
}
export interface VertexTypes {
  aVertex: {
    aProperty: string;
  };
  anotherVertex: {
    bProperty: number;
  };
}
export interface EdgeTypes {
  anEdge: {
    aProperty: string;
  };
  anotherEdge: {
    bProperty: number;
  };
}
